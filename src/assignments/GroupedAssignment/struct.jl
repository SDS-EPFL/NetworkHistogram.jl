# assignment that move the edge data around when trying an update
# might be useful if the computation of the loglikelihood is expensive
# and with no closed-form solution: contiguous memory access might be faster
